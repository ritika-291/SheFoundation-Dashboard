# MongoDB Connection String
# Replace with your MongoDB Atlas connection string or use local MongoDB
MONGODB_URI=mongodb://localhost:27017/intern-dashboard

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: MongoDB Atlas connection string (uncomment and replace with your actual connection string)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/intern-dashboard?retryWrites=true&w=majority 